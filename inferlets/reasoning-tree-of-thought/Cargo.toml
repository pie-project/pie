[package]
name = "reasoning-tree-of-thought"
version = "0.1.0"
edition = "2024"

[lib]
crate-type = ["cdylib"]

[dependencies]
inferlet = { path = "../../sdk/rust/inferlet" }
reasoning-core = { path = "../reasoning-core" }

[profile.release]
lto = true
