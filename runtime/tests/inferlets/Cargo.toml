[workspace]
resolver = "2"
members = [
    "echo",
    "context",
    "error",
    "generate",
    "grammar",
    "grammar-late",
    "mirostat",
    "tempgen",
    "multisamp",
    "isolatedtopp",
    "runahead",
]

[workspace.dependencies]
inferlet = { path = "../../../sdk/rust/inferlet" }
inferlet-macros = { path = "../../../sdk/rust/inferlet-macros" }
