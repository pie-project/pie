[package]
name = "plex-paper-marconi"
version = "0.1.0"
edition = "2024"
publish = false

[lib]
crate-type = ["cdylib"]
path = "src/lib.rs"

[dependencies]
paper-common = { package = "plex-paper-common", path = "../paper-common" }
plex = { package = "pie-plex-sdk", path = "../../../sdk/rust/plex" }
