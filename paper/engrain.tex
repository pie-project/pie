%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
% engrain -- MLSys 2027 submission
%
% Every number in this paper is produced by a harness in this repository and
% names its artifact in the caption. Numbers that are stale with respect to the
% current implementation are not quoted at all; see paper/PROVENANCE.md.
%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%

\documentclass{article}

\usepackage{microtype}
\usepackage{graphicx}
\usepackage{subfigure}
\usepackage{booktabs}
\usepackage{amsmath}
\usepackage{amssymb}
\usepackage{multirow}
\usepackage{hyperref}

\newcommand{\theHalgorithm}{\arabic{algorithm}}

\usepackage{mlsys2025}

\mlsystitlerunning{Engrain: Device-Resident LR(1) Constrained Decoding}

\begin{document}

\twocolumn[
\mlsystitle{Engrain: Constrained Decoding with the Parser Resident on the GPU}

\begin{mlsysauthorlist}
\mlsysauthor{Anonymous Authors}{anon}
\end{mlsysauthorlist}

\mlsysaffiliation{anon}{Anonymous Institution}

\mlsyskeywords{constrained decoding, structured generation, LR parsing, CUDA
graphs, LLM serving}

\vskip 0.3in

\begin{abstract}
Constrained decoding restricts a language model to a formal language by masking
the logits at every step, and every deployed implementation computes that mask
on the host. This is not an efficiency detail: a CUDA graph is a recorded
sequence of \emph{device} work, so a component that requires the host mid-step
cannot be inside the graph a serving engine replays for its decode loop, cannot
hand an allowed set to a fused sampler, and cannot follow a speculative draft
without a synchronization per position. We present \textbf{engrain}, a
constrained-decoding engine whose LALR(1) parser state --- lexer state, parser
stack, and conflict forks --- lives in GPU memory and advances in device
kernels, so that the grammar is part of the decode step rather than an input to
it. The design contributes an exact device encoding of the (lexer
state $\times$ parser stack) configuration space, a mask fill that is a single
launch for an arbitrarily heterogeneous batch, and a cross-step memo that
answers a third of all steps without touching the tables. On an A100 with a
151{,}669-token vocabulary, a full constrained step (mask \emph{and} parser
advance) costs 348--3{,}781\,\textmu{}s at batch 512 across three schemas
against XGrammar's 1{,}743--3{,}382 and llguidance's 769--797, adds
90\,\textmu{}s to an overlapped decode step where a host matcher adds 398, and
holds its p99 flat when the host loses cores to other work while the host
matcher's p99 rises 7.8$\times$. Masks are bit-exact against a reference
matcher over 7{,}289 verified rows. Because no two engines lower a schema to
the same language --- of 200 corpus schemas, \emph{zero} agree with XGrammar
token for token --- we construct the common ground: 468 real schemas rewritten
into the fragment engrain enforces exactly, membership verified by generating
10{,}177 documents under their masks and validating every one. On it XGrammar
has two settings and neither is both sound and complete: its default refuses
91.7\% of valid documents whose object keys are permuted, and its order-free
setting does not enforce \texttt{required} at all, admitting invalid documents
26.3\% of the time. llguidance is sound but fixes property order with no
setting to undo it, so \textbf{engrain is the only engine here that is both
sound and order-free} --- and that, rather than soundness, is the correctness
claim. End to end against XGrammar's order-free setting we are 1.72$\times$
faster while producing 49\% more valid documents, and against llguidance
1.13$\times$ at batch 512. We also report where residency is paid for:
llguidance beats us end to end through batch 128 and serves a single request in
half our time, our cold path to a new schema's first mask is 227\,ms against
its 1.2, and on an 8B model the speed difference vanishes entirely --- a host
fill hides behind a longer forward pass and a device fill cannot --- leaving
only the correctness gap.
\end{abstract}
]

\printAffiliationsAndNotice{}

\section{Introduction}
\label{sec:intro}

Structured generation --- forcing a language model's output to be valid JSON,
to match a schema, or to parse under a grammar --- is now a standard serving
feature. It is implemented by \emph{constrained decoding}: before each token is
sampled, the engine computes a bitmask over the vocabulary marking which tokens
can extend the output without leaving the language, and sets the rest to
$-\infty$.

Every widely deployed implementation --- XGrammar~\cite{dong2024xgrammar},
llguidance, Outlines~\cite{willard2023efficient}, SynCode~\cite{ugare2025syncode}
--- computes that mask with a CPU automaton and copies it to the device. The
choice looks reasonable: the mask is small, the automaton step is a few
microseconds, and the copy overlaps the forward pass. Measured as a fraction of
a decode step, it is 4.6\% at batch 512 on a 0.6B model, and an engine that
made it twice as fast would be 2.3\% faster end to end. Treated as a latency
problem, constrained decoding is nearly finished.

This paper argues that latency is the wrong frame, and that the interesting
property is \emph{where the state lives}.

Modern serving engines record their decode step as a CUDA graph and replay it,
because at batch 512 the per-kernel launch cost otherwise dominates. A CUDA
graph is a fixed, recorded sequence of device work. Anything the host has to
\emph{produce} in the middle of a step cannot be recorded into it --- attempting
to capture a host-side mask fill yields a graph that contains none of the work,
and replaying it reproduces whatever the host buffer happened to hold last.
Consequently, every deployed constrained decoder sits \emph{outside} the graph
and hands a buffer in.

Three things follow, and none of them is a speed argument:

\begin{itemize}
\item \textbf{The decode loop cannot be captured whole.} A host-dependent
component cannot join a fully graph-resident or megakernel decode loop at all.
\item \textbf{The sampler cannot be fused with the constraint.} If the allowed
set is a device object, a sampler can read the few hundred candidates the
grammar permits instead of all 151{,}669 logits. If it is produced on the host,
the only interface is a vocabulary-wide mask.
\item \textbf{Speculative decoding needs a host round trip per draft position},
or a specialized batch API, because the parser cannot be advanced and rolled
back inside the same recorded step.
\end{itemize}

On each of these the comparison is not ``we are faster'' but ``the other design
cannot participate''. That is the claim this paper makes, and the engineering it
requires is the paper's substance: moving an LR parser onto a GPU is not a port.

\paragraph{Why an LR parser, and why that is hard.} A grammar-constrained
decoder must answer, at every step, ``which of the model's tokens may come
next''. Model tokens are byte strings from a BPE vocabulary; they straddle
lexeme boundaries, so a token may end in the middle of a number or a string. The
parser configuration must therefore carry a \emph{lexer} state as well as a
parser stack. Worse, an LR parser's next state after a reduction depends on the
stack below the popped states, so \texttt{next[state, token]} is not a function
of the state alone and no flat per-state table is exact. Prior GPU work avoids
this by restricting the grammar class: an acyclic schema DFA, a bounded stack
depth, or a conservative stack abstraction. Restricting the class is precisely
what makes those systems unable to serve the grammars people write.

\paragraph{Contributions.} (i) An exact device representation of the (lexer
state $\times$ parser stack) configuration space for LALR(1) grammars, with
runtime forking on reduce/reduce conflicts, so no bound truncates the accepted
language (\S\ref{sec:automaton}). (ii) A mask fill that is a single launch for
a batch of arbitrarily many distinct grammars at arbitrarily different document
positions, built as a \emph{probe} that discovers per-sequence work and a
\emph{sweep} that redistributes it --- with evidence that the obvious
single-kernel fusion is 14$\times$ slower on the batch shape serving actually
produces (\S\ref{sec:fill}). (iii) A cross-step memo that answers 86--100\% of
steps from a previously computed row (\S\ref{sec:memo}). (iv) Fused
constrained sampling, with an honest account of why its win does not survive a
ragged batch (\S\ref{sec:sampling}). (v) A comparison methodology for
constrained decoders, since none of them lower a schema to the same language:
construct the fragment on which an engine is exact, verify membership by
generating and validating rather than by asserting, and compare there --- which
turns ``which is faster'' into a question with an answer, and located
correctness defects in both engines, including four in our own
(\S\ref{sec:correctness}). (vi) An evaluation that reports the costs of
residency --- the cold path, memory, small-batch latency, and the model size at
which our throughput advantage disappears --- as first-class results
(\S\ref{sec:eval-e2e},~\S\ref{sec:costs}).

\section{Background and Motivation}
\label{sec:background}

\subsection{Constrained decoding}

Given a grammar $G$ and a model vocabulary $V$ of byte strings, a constrained
decoder maintains an automaton state per sequence and computes at each step the
set $A \subseteq V$ of tokens that keep the output a prefix of some string in
$L(G)$; the mask is the indicator of $A$.

XGrammar~\cite{dong2024xgrammar} --- the most widely deployed implementation,
and our baseline throughout --- compiles the grammar to a pushdown automaton,
precomputes for each automaton state an ``adaptive token mask'' partitioned into
accepted and rejected sets, and at each step walks the per-sequence stack on the
CPU to fill a bitmask, which is copied to the device and applied by a small
kernel. Its compiler cache is kilobyte-scale and its compile time is
milliseconds, both of which are properties we do not match and report honestly
in \S\ref{sec:costs}.

\subsection{The graph boundary}

At batch 512, a decode step of a small model issues hundreds of kernels whose
individual launch overheads exceed their execution times. Serving engines
therefore record the step once as a CUDA graph and replay it. Capture is
\emph{stream capture}: the operations enqueued on a stream between
\texttt{cudaStreamBeginCapture} and \texttt{cudaStreamEndCapture} become graph
nodes. Host code executes normally during capture and contributes nothing.

This makes the boundary sharp rather than gradual. A constrained decoder that
computes its mask on the host is not ``slower inside a graph''; it is absent
from the graph, and the replayed step silently uses a stale buffer. The only
sound arrangement is the one deployed today: keep the fill outside, synchronize,
hand in a buffer. Every property in \S\ref{sec:intro} follows from that
arrangement, not from the fill's cost.

\subsection{What the workload actually looks like}

Two properties of real constrained decoding shape the design, and both are
measured over model-generated documents rather than assumed. We generate 533
JSON values with Llama-3-8B-Instruct under an XGrammar constraint (50 schemas
from each of the 11 JSONSchemaBench~\cite{geng2025jsonschemabench} configs,
temperature 0.8, top-$p$ 0.95, mean 125 tokens) and replay the text through
each tokenizer's grammar, which is faithful because the state a matcher reaches
is determined by the bytes consumed.

The first property is that \textbf{width is set by the schema, not the
vocabulary}: over 55{,}406 / 66{,}557 / 69{,}313 real steps the median step
allows 396 / 378 / 107 tokens for Llama 3 (128k), Qwen 3.6 (248k) and Gemma 4
(262k), while 32.0--32.9\% of steps allow more than 8{,}192. A structural position (after a comma, inside an
object) allows a few hundred tokens whatever the vocabulary size, so the ratio
between an $O(|V|)$ mask and an $O(|A|)$ allowed set \emph{grows} as vocabularies
grow. The second property is that the distribution is bimodal: string bodies
allow nearly everything, and a third of all steps are in one. Any design that
assumes narrow rows --- including our own fused sampler --- meets that third of
steps, and \S\ref{sec:sampling} reports what happens when it does.

The third property caused the largest single regression in this project: a
serving batch is \emph{skewed by construction}. Continuous batching admits
requests at different times, so every sequence sits at its own position in its
own document and the parser work each needs differs by an order of magnitude.
Benchmarks that place every sequence in the same parse state measure the one
arrangement that flatters a design which deduplicates; all measurements here
seed each sequence at a random position.

\section{Design}
\label{sec:design}

\subsection{The automaton, and where it ends}
\label{sec:automaton}

A configuration is a pair (lexer DFA state, parser stack). The compiler produces,
from a JSON Schema, EBNF or regex front end: an LALR(1) ACTION/GOTO table over
grammar terminals; a byte-level lexer DFA whose accepting states name terminals;
and a partition of the model vocabulary into \emph{token groups}.

\paragraph{Token groups.} The bridge between BPE tokens and grammar terminals is
the expensive object. For each lexer state $\ell$ and each token $t$, feeding
$t$'s bytes from $\ell$ either fails, or ends in some lexer state $\ell'$ having
emitted a (possibly empty) sequence of terminals. Two tokens that do the same
thing from \emph{every} lexer state are interchangeable for masking purposes, so
the vocabulary is quotiented by that equivalence. Real vocabularies collapse
hard: 151{,}669 Qwen3 tokens become a few hundred groups per grammar. The mask
fill then works over groups, and expands to tokens only at the end.

\paragraph{Stacks, exactly.} The classical obstacle is that a reduction pops
$k$ states and the following GOTO depends on what is exposed underneath, so a
flat \texttt{next[state, token]} table is not exact. Bounding the stack depth
and enumerating reachable stacks --- the approach a prototype of this system
took --- both explodes (compile timed out beyond depth 6 at a 4{,}096-token
vocabulary) and silently makes the accepted language a strict subset of the
grammar when the bound is exceeded. That is a correctness cliff, not a
performance one.

Engrain keeps the stack. A sequence's device state is a bounded-capacity array
of configurations, each holding a lexer state and an explicit parser stack, and
the advance kernel performs real reductions with real GOTO lookups. What is
bounded is the number of \emph{simultaneous} configurations, not the depth of
any stack, and exceeding that bound is reported as a typed refusal at compile
time rather than silently narrowing the language.

\paragraph{Conflicts, forked at runtime.} LALR(1) construction on lowered JSON
Schema produces reduce/reduce conflicts --- \texttt{oneOf} in 86\% of the
conflicted grammars. Rather than refuse those grammars (which would cost a fifth
of the corpus) or promote the whole system to full GLR, engrain forks the
conflicted configuration at runtime and carries both, pruning any fork that dies
on the next terminal. This is GLR-lite: unbounded in principle, bounded by the
configuration capacity in practice, and the capacity is checked rather than
assumed.

\subsection{The fill: a probe and a sweep}
\label{sec:fill}

The mask fill is the step's device-side work. Its input is, per sequence, a set
of configurations; its output is a bitmask row over the vocabulary. Conceptually
it is: for each sequence, for each configuration, for each token group reachable
from that configuration's lexer state, if the group's terminal is accepted by
ACTION in the configuration's parser state, OR the group's token bits into the
row.

The iteration space is therefore ragged in three dimensions at once ---
sequences have different numbers of configurations, configurations have
different numbers of groups, and groups have different numbers of tokens. Our
first CUDA implementation fused the whole thing into one kernel with one block
per sequence. On a balanced batch it was 2.3--4.5$\times$ faster than the Triton
implementation it replaced. On a \emph{skewed} batch --- the shape serving
actually produces --- it was \textbf{16.4$\times$ slower} (9{,}883\,\textmu{}s
against 604\,\textmu{}s), because one block per sequence makes the fill cost the
\emph{widest} sequence rather than the total work. Measuring a fused kernel on a
balanced batch is not measuring it.

The fix is two kernels, and the reason it must be two is a hazard rather than a
preference:

\begin{itemize}
\item \textbf{Probe} (one block per sequence) clears the sequence's row and
records how many (configuration, group) items it will contribute.
\item \textbf{Sweep} (grid $(\text{batch}, \text{chunks})$) walks a
\emph{flattened} (configuration, group) iteration space, so a sequence with 64
configurations of one group each is spread over blocks rather than serialized
into one.
\end{itemize}

They cannot be merged: the probe clears rows that the sweep ORs into, and a
block clearing a row while another accumulates into it loses bits. The chunk
count is bounded by a scratch budget rather than by the batch --- a thread owns
two replay windows, so the buffer is $\text{batch} \times \text{chunks} \times
\text{threads}$, which is 268\,MB at batch 512 with 8 chunks. A 64\,MiB budget
gives 32 chunks at batch 32 and 2 at batch 512.

After the split the skewed step fell from 9{,}883\,\textmu{}s to
641\,\textmu{}s, a 15.4$\times$ improvement, and the remaining gap to our own
Triton reference narrowed from 1.16--3.29$\times$ to 1.16--1.31$\times$.

\subsection{The advance}

After a token is sampled, each sequence's configurations must be advanced by it.
This is the half that cannot be hidden behind the forward pass, because it
follows the sampled token. Engrain fuses the whole advance --- feeding the
token's bytes through the lexer, running reductions and GOTOs, forking on
conflicts, compacting dead configurations, and writing the rollback history
entry --- into a single kernel. The rollback path went from 16--20 graph nodes
to 7 by folding the history write inline, removing a counting kernel and a
$\text{grid}=(1,)$ prefix sum.

\subsection{Cross-step memoisation}
\label{sec:memo}

Sequences repeat states. Within one request, consecutive steps often reach a
configuration set that has been seen before; across a batch under one grammar,
many sequences share one. Engrain hashes the configuration set on device --- one
warp per configuration, folding stacks in shared memory --- and looks it up in a
device-resident memo of previously computed rows. Over a real walk the memo
answers \textbf{35.6\%} of steps without reading a table.

The hash itself was a bottleneck until it was parallelized: one warp performing
64 sequential stack reductions cost 65.8\,\textmu{}s; one warp per configuration
costs 16.3\,\textmu{}s, half of the Triton reference's 32.8.

\subsection{Fused constrained sampling}
\label{sec:sampling}

Because the allowed set is a device object, the sampler can read it directly.
A \texttt{compact} kernel emits, per row, a sorted list of allowed token ids
using a block-wide scan. The sampler then gathers a few hundred logits instead
of reading all 151{,}669.

The honest accounting of this idea is in \S\ref{sec:eval-sampling}, and it is
not the one this paper originally claimed. Compaction wins by 7.7$\times$ on a
sparse row and \emph{loses} by 0.95$\times$ on a dense one, so a fixed policy is
a net loss on the real bimodal distribution; a per-step choice made on device
(emit whichever of the allowed set and its complement is shorter) recovers
1.79$\times$; and on a genuinely ragged batch --- half the rows dense, which is
the measured steady state --- the advantage falls to 1.05$\times$. The reason is
not in the parser: it is that XGrammar's mask-apply takes its row indices as a
host \texttt{List[int]}, and a sampler's output shape is its row count, so any
subset scheme needs a host synchronization that the residency claim exists to
avoid.

\subsection{Arena and paging}

All grammars the engine has seen live in one device arena and a sequence
carries the index of the one it is under, which is what makes a batch of
distinct grammars a single launch. Reclaiming memory under continuous batching
has to happen \emph{without moving anything}: compaction renumbers survivors,
so every recorded graph would have to be re-recorded, giving back exactly what
residency bought. Each array therefore carries a coalescing free list and
identifiers are recycled rather than renumbered, with any grammar a live
sequence is running under pinned. Across 400 admissions of 40 schemas at a
32\,MB budget, 399 evictions cost \textbf{zero} graph re-records.

\section{Implementation}
\label{sec:impl}

The compiler is 7 Rust crates ($\approx$\,20k lines): front ends (JSON Schema,
EBNF, regex), lexer construction and vocabulary grouping, LALR(1) table
construction, artifact emission, and a reference matcher. The device runtime is
CUDA compiled to a fatbin for sm\_80--100 plus forward-compatible PTX, embedded
in the extension so that no driver JIT happens at load; the Python layer is
$\approx$\,5k lines over PyO3 bindings.

The system keeps two complete backends: the CUDA kernels, and a Triton
implementation of the same algorithms that is retained as an executable
reference. A third \texttt{differential} backend runs both on every call and
compares, which is how kernel-level divergence is caught. All results in this
paper are from the CUDA backend unless stated.

\section{Experimental Design}
\label{sec:design-exp}

A systems paper's claims are only as good as the angles from which someone
tried to break them. We state each claim with the measurement that would
falsify it, and report all of them --- including the two that came out against
us and the one that found a bug in our own compiler.

Concretely: a graph recorded on one grammar assignment must replay correctly on
another, or residency is not real; XGrammar's fill must \emph{not} overlap the
forward pass so completely that its added cost equals ours; the host matcher's
tail must \emph{not} stay flat as CPU cores are taken away; a better-tuned
XGrammar or llguidance must not close the per-step gap; removing the memo or
capture must change something; cost must not grow with the number of distinct
schemas in a batch; no device row may differ from the reference matcher; and no
document the grammar accepts may be one the schema rejects. Every one of these
is reported in \S\ref{sec:eval}, including the three that came out against us.

Two design rules apply throughout. \textbf{Both halves are charged}: a step is a
mask fill \emph{and} a parser advance, and reporting only the fill flatters
whichever engine has the cheaper one. \textbf{Batches are skewed}: every
sequence is seeded at a random position in its own document, because a balanced
batch is the single arrangement that flatters a design which deduplicates, and
measuring an ablation on one would understate every mechanism at once.

\subsection{Baselines, and giving each of them its best}
\label{sec:baselines}

We compare against three systems, each given its strongest interface:

\begin{itemize}
\item \textbf{XGrammar}~\cite{dong2024xgrammar}: a pushdown automaton with
adaptive token masks, using \texttt{BatchGrammarMatcher} with the thread count
swept over $\{1,2,4,8,16\}$ and the best reported, and \texttt{any\_order=True}
(see below).
\item \textbf{llguidance}: an Earley parser over a lark grammar, using its
parallel executor path (\texttt{fill\_next\_token\_bit\-mask\_par} and
\texttt{consume\_token\_par}) rather than a Python loop.
\item \textbf{outlines\_core}: JSON Schema lowered to a \emph{regular
expression} and compiled to a DFA indexed by the vocabulary. It has no batch
interface at all --- \texttt{allocate\_token\_bitmask} takes a vocabulary size,
not a batch --- so a batch is a Python loop, which we report as a property of
the system rather than hide behind a nicer loop.
\end{itemize}

\paragraph{A fairness correction we had to make.} Our first comparison used
XGrammar's default, which fixes object property order at the schema's
declaration: with \texttt{properties: \{a, b\}} it accepts
\texttt{\{"a":1,"b":2\}} and \emph{rejects} \texttt{\{"b":2,"a":1\}}, though
both are valid. Since engrain is order-free by construction, that comparison
was against a strictly weaker configuration. XGrammar has an
\texttt{any\_order} flag; enabling it accepts both orders and, on 60 corpus
schemas, \emph{also compiles faster} --- 0.81\,s against 4.89\,s. It is
strictly its better setting and all XGrammar microbenchmarks in this paper use
it.

It is also, we later found, not a setting anyone deploying gets:
\texttt{any\_order} is not exposed by vLLM, so a served request always uses the
ordered lowering. That matters more than a fairness correction, because the two
settings differ in \emph{which documents they accept} and in opposite
directions --- \S\ref{sec:correctness} measures both, and the end-to-end runs
of \S\ref{sec:eval-e2e} report the default and the matched configuration side
by side, the latter through a one-line patch to vLLM that exposes the flag.

\paragraph{The corpus.} Latency runs use JSONSchemaBench directly. Every
correctness and end-to-end result uses the constructed fragment of
\S\ref{sec:correctness}: 468 of the 533 schemas, rewritten so that engrain
enforces them with nothing left over, and filtered to those XGrammar also
compiles. It is favourable to us by construction and we say so wherever it is
used; the alternative --- comparing two engines on schemas they lower to
different languages --- is not a comparison of engines at all.

\paragraph{The same question, asked of the others.} outlines\_core cannot be
configured out of this: its lowering emits the literal regex
\verb|\{"a":...,"b":...\}|, so property order is a property of the language it
accepts. It \emph{under}-approximates --- it blocks documents the schema
permits --- which is the opposite failure from ours, and the one a user
experiences as the model being unable to finish. In our latency runs
outlines\_core could not even be seeded into a live state on any of the three
model-generated documents the other three engines accept.

\section{Evaluation}
\label{sec:eval}

\subsection{Setup}

All measurements are on one NVIDIA A100 80GB PCIe with Qwen3-0.6B's
151{,}669-token vocabulary unless stated, and use three JSONSchemaBench schemas
of increasing difficulty (schema 0, 1, 2). Both engines are charged for
\emph{both} halves of a step --- the mask fill and the parser advance --- because
reporting only the fill flatters whichever engine has the cheaper one.
XGrammar's thread count is swept over $\{1,2,4,8,16,\text{auto}\}$ and its best
is reported; its device work is CUDA-graphed, as ours is. Every sequence is
seeded at a random position in its document. We report medians of 100--200
repeats after warm-up.

\subsection{Step latency}
\label{sec:eval-step}

Table~\ref{tab:step} is the paper's central performance result and also its
clearest limitation. At batch 1 we lose on all three schemas; the crossover is
between batch 8 and 32 depending on schema; above it the gap widens
monotonically to 12.8--20.8$\times$ at batch 512.

\begin{table}[t]
\caption{Full constrained step (fill + advance), median \textmu{}s, three
schemas, sequences at random document positions. XGrammar uses
\texttt{any\_order=True} and its best thread count; llguidance uses its
parallel executor. Host engines are charged the copy of the mask to the device;
ours never leaves it. Source: \texttt{exp\_baselines\_latency}, 2026-07-31.}
\label{tab:step}
\vskip 0.1in
\begin{center}
\begin{small}
\begin{tabular}{rrrrr}
\toprule
batch & schema & engrain & XGrammar & llguidance \\
\midrule
\multirow{3}{*}{1}
 & 0 & 50.3 & \textbf{27.9} & 20.4 \\
 & 1 & 49.8 & 28.1 & \textbf{20.1} \\
 & 2 & 46.4 & 28.0 & \textbf{21.2} \\
\midrule
\multirow{3}{*}{8}
 & 0 & 190.8 & \textbf{52.1} & 216.8 \\
 & 1 & 192.5 & \textbf{200.2} & 329.5 \\
 & 2 & \textbf{47.2} & 128.3 & 260.2 \\
\midrule
\multirow{3}{*}{32}
 & 0 & 196.0 & \textbf{126.5} & 193.9 \\
 & 1 & 253.3 & 558.5 & \textbf{218.2} \\
 & 2 & \textbf{53.2} & 395.2 & 230.7 \\
\midrule
\multirow{3}{*}{128}
 & 0 & \textbf{199.2} & 463.6 & 390.2 \\
 & 1 & 1{,}161.2 & 1{,}173.7 & \textbf{321.6} \\
 & 2 & \textbf{371.8} & 955.2 & 393.3 \\
\midrule
\multirow{3}{*}{512}
 & 0 & \textbf{347.7} & 1{,}742.5 & 797.2 \\
 & 1 & 3{,}780.7 & 3{,}294.3 & \textbf{789.7} \\
 & 2 & \textbf{393.5} & 3{,}382.5 & 769.0 \\
\bottomrule
\end{tabular}
\end{small}
\end{center}
\vskip -0.1in
\end{table}

Three readings matter, and one of them is against us.

\textbf{Against XGrammar the shape is structural.} Its cost is per sequence and
scales with the batch; ours is a fixed number of launches whose work scales
with \emph{distinct} parse states. At batch 512 we are 5.0$\times$ and
8.6$\times$ cheaper on schemas 0 and 2. Enabling \texttt{any\_order} did not
change this: its per-step cost is within noise of the default
(1{,}742/3{,}294/3{,}382 against 1{,}846/3{,}156/3{,}370), so the fairness
correction cost us nothing to make.

\textbf{llguidance is a much stronger baseline than XGrammar, and it is flat.}
At batch 512 it costs 769--797\,\textmu{}s \emph{whatever the schema}, where we
range from 348 to 3{,}781. Its Earley formulation does no vocabulary-indexed
precomputation, so its cost tracks the batch and not the grammar. We beat it
2.3$\times$ and 1.9$\times$ on schemas 0 and 2 --- and \textbf{lose to it
4.8$\times$ on schema 1}, where our sweep is dominated by the number of
distinct configurations the skewed batch reaches. A paper that compared only
against XGrammar would have reported a 5--9$\times$ win and missed this
entirely.

\textbf{Small batch is a loss.} At batch 1 we are 46--50\,\textmu{}s against
20--28: our floor is set by sweeping every live configuration's groups whatever
the batch. We do not claim a small-batch win, and a deployment that runs at
batch 1 should not use this system.

Our CUDA backend is also 1.16--1.32$\times$ behind our own Triton
implementation of the same algorithms on the hardest schema --- a per-item
throughput gap in the sweep, not load imbalance --- which we report because an
implementation that loses to its own reference has not finished.

\subsection{Coverage and compile time across engines}
\label{sec:eval-coverage}

A latency table over schemas everyone compiles hides the denominator: a narrow
engine looks fast by declining hard cases. Table~\ref{tab:coverage} measures
that denominator over all 528 corpus schemas.

\begin{table}[t]
\caption{Coverage and compile time over 528 JSONSchemaBench schemas, tokenizer
object shared across schemas as a server would hold it.
Source: \texttt{exp\_baselines\_coverage}, 2026-07-31.}
\label{tab:coverage}
\vskip 0.1in
\begin{center}
\begin{small}
\begin{tabular}{lrrrr}
\toprule
engine & coverage & p50 & p90 & p99 \\
\midrule
engrain    & 95.3\% & 123.6\,ms & 1{,}489\,ms & 4{,}763\,ms \\
XGrammar   & \textbf{100.0\%} & 3.5\,ms & 116\,ms & 1{,}240\,ms \\
llguidance & 84.1\% & \textbf{1.1\,ms} & \textbf{4.6\,ms} & \textbf{20\,ms} \\
\bottomrule
\end{tabular}
\end{small}
\end{center}
\vskip -0.1in
\end{table}

This is the axis on which we are worst and it deserves to be stated plainly.
\textbf{llguidance compiles 112$\times$ faster than us at the median} and
238$\times$ faster at p99, because an Earley parser needs no vocabulary-indexed
tables at all --- it pays per step what we pay once. XGrammar compiles every
schema in the corpus and is 35$\times$ faster than us at the median. Our
distribution also has an outlier we have not attributed: a single schema whose
compile ran for roughly half an hour, far beyond the 4.8\,s p99.

The compensation is coverage relative to llguidance, which refuses 84 of the
528: \texttt{oneOf} (31), \texttt{dependencies} (15), \texttt{uniqueItems} (9)
and \texttt{not} (6) are unimplemented in its JSON Schema front end. Our 25
refusals are 12 the front end cannot lower, 10 past the production budget and
3 past the lexer budget. outlines\_core is not in this table because its
regex lowering could not be seeded into a live state on any of the three
model-generated documents used for Table~\ref{tab:step}, and building its
vocabulary-indexed DFA cost 33.8\,s over 20 schemas against our 1.6\,s.

The honest summary of Tables~\ref{tab:step} and \ref{tab:coverage} is a
design-space tradeoff rather than a victory: llguidance pays nothing to compile
and a flat price per step; we pay a large and occasionally terrible price to
compile, and a per-step price that is lower on two of three schemas and much
worse on the third. What neither baseline can do at any price is be inside the
decode graph.

\subsection{Heterogeneous batches}
\label{sec:eval-mixed}

The case the design exists for is a batch of \emph{different} grammars, which is
what continuous batching produces and what should hurt us: our fill deduplicates
rows that share a grammar, a lexer state and a stack, and a mixture has fewer
such rows to share, while our buffers are sized by maxima over the whole pool.
XGrammar's per-sequence host call is schema-agnostic, so mixing costs it nothing
structurally.

\begin{table}[t]
\caption{Cost of a batch drawn from $n$ distinct schemas, median \textmu{}s, and
the cost of mixing itself: the ratio of the mixed batch to the mean of the same
schemas run alone at the same batch size.
Source: \texttt{probe\_mixed\_cost}, 2026-07-31.}
\label{tab:mixed}
\vskip 0.1in
\begin{center}
\begin{small}
\begin{tabular}{rrrrrr}
\toprule
batch & $n$ & engrain & XGrammar & ratio & mix costs us \\
\midrule
\multirow{5}{*}{32}
 & 1  & 82    & 166    & 2.01$\times$ & 0.99$\times$ \\
 & 2  & 388   & 181    & 0.47$\times$ & 5.00$\times$ \\
 & 4  & 401   & 398    & 0.99$\times$ & 5.38$\times$ \\
 & 8  & 442   & 1{,}991 & 4.51$\times$ & 5.50$\times$ \\
 & 16 & 394   & 1{,}086 & 2.76$\times$ & 5.19$\times$ \\
\multirow{5}{*}{512}
 & 1  & 114   & 1{,}930 & 16.89$\times$ & 0.99$\times$ \\
 & 2  & 419   & 2{,}456 & 5.86$\times$ & 3.79$\times$ \\
 & 4  & 430   & 6{,}604 & 15.37$\times$ & 4.02$\times$ \\
 & 8  & 4{,}025 & 35{,}781 & 8.89$\times$ & 33.17$\times$ \\
 & 16 & 4{,}052 & 44{,}963 & 11.10$\times$ & 35.98$\times$ \\
\bottomrule
\end{tabular}
\end{small}
\end{center}
\vskip -0.1in
\end{table}

Table~\ref{tab:mixed} reports both the comparison and the control. Three
findings, one of them against us:

\textbf{Heterogeneity is a step, not a slope.} At batch 32, one grammar costs
82\,\textmu{}s and two cost 388; but sixteen also cost 394. The 5$\times$ is paid
once, when the pool stops being homogeneous, and then does not grow with the
mixture. This is the property that matters for serving, where the mixture is
never one.

\textbf{We lose at two schemas and small batch.} 0.47$\times$ at batch 32 with
two schemas is the worst cell in the table, and it is the direct consequence of
the step above: we pay the full heterogeneity cost while XGrammar still has only
64 cheap host calls to make.

At batch 128 (omitted for space) the picture is the same, 1.70--15.50$\times$.
\textbf{The absolute cost at 8--16 schemas and batch 512 is bad.} 4\,ms is
33--36$\times$ what the same schemas cost run alone, against 0.82--1.04$\times$
for XGrammar. We still win the comparison 8.9--11.1$\times$ because their
absolute cost is 36--45\,ms, but the honest statement is that pool-wide ceilings
--- the replay window, the readings a group may have --- are sized by the
largest member and every sequence pays for it. This is the clearest target for
future work.

\subsection{End to end, and what a fair comparison requires}
\label{sec:eval-e2e}

Microbenchmarks of a mask do not settle a serving question, so we run both
engines inside vLLM on the fragment of \S\ref{sec:correctness}, where neither
is allowed to be wrong. Three methodological corrections were needed first, and
each of them moved a number we had already reported.

\paragraph{Quote the makespan, not tokens per second.}
$\text{tok/s} = \text{tokens}/\text{makespan}$, so an engine whose requests run
longer generates more tokens \emph{and} keeps the batch fuller, and a fuller
batch spreads the per-step fixed cost over more rows. Ours run longer: 321 of
512 reach the token cap against XGrammar's 223. The inflation is exactly the
gap in tokens generated---17.7\% more tokens, and a tokens-per-second ratio
17\% better than the makespan ratio. Capping generation short enough that both
engines are nearly all capped collapses the work gap to 2.5\% and the two
metrics agree to within 3\%. Both arms serve the same 512 requests, so how long
that takes needs no normalising, and it is what we report.

\paragraph{Thread the baseline where the server threads it.} vLLM fills
XGrammar's rows on a thread pool only above 128 structured requests in a step,
in chunks of sixteen. Threading it at every batch---which our harness did---is
wrong in both directions: at batch 32 the pool costs more than the work, and at
128 it makes XGrammar look faster than vLLM runs it.

\paragraph{Match the semantics.} Since \texttt{any\_order} is not exposed, we
patched vLLM to expose it. The default column below is what a user gets today;
the matched column is the comparison of two engines on one language.

\begin{table}[t]
\caption{vLLM, Qwen3-0.6B, batch 512, 409 distinct schemas, 96-token cap.
Makespan is the time to serve all 512 requests. Source: \texttt{rigor.e2e}.}
\label{tab:e2e}
\vskip 0.1in
\begin{center}
\begin{small}
\begin{tabular}{lrrr}
\toprule
 & makespan & tokens & valid \\
\midrule
unconstrained & 1.66\,s & 49{,}145 & 0/512 \\
\textbf{engrain} & \textbf{3.30\,s} & 37{,}488 & 200/512 \\
llguidance & 3.71\,s & 32{,}310 & 297/512 \\
XGrammar, ordered & 6.08\,s & 31{,}859 & 290/512 \\
XGrammar, unordered & 5.68\,s & 38{,}453 & 134/512 \\
\bottomrule
\end{tabular}
\end{small}
\end{center}
\vskip -0.1in
\end{table}

Against XGrammar's matched configuration we are \textbf{1.72$\times$ faster and
produce 49\% more valid documents} (Table~\ref{tab:e2e}). Against llguidance,
which is the strongest baseline and beats XGrammar at every batch size, we are
1.13$\times$ faster at batch 512 and 1.30$\times$ per generated token. The high
validity of the two order-fixed rows is not greater correctness; it is the
narrowing---forcing the declared order makes a document finish sooner and land
inside the cap, which is why they also generate 14--16\% fewer tokens, and it
is the same constraint that refuses over 91\% of permuted valid documents.

\paragraph{Where the crossover is, and it depends on the baseline.} With
vLLM's own threading rule the mask crossover against XGrammar is batch
$\approx$30: at 8 they are 2.8$\times$ cheaper, at 32 the two are level, at 128
we are 3.3$\times$ cheaper and at 512 5.1$\times$. \textbf{Against llguidance
it is about batch 160.} End to end it serves a single request in 0.248\,s
against our 0.469, and stays ahead through batch 128 (0.898 against 0.953)
before we take 256 (1.380 against 1.721) and 512. Below a hundred-odd
concurrent requests a host matcher is simply cheaper than a kernel launch, and
nothing in this design changes that.

\paragraph{Schema diversity helps us, which we did not expect.} Our fill
deduplicates rows sharing a grammar \emph{and} a parse state, and a host
matcher's per-row call is schema-agnostic, so a batch drawing on many schemas
should close the gap. It widens it: 1.28$\times$ at one schema, 1.40$\times$ at
64, 1.85$\times$ at 409. Going from one schema to 409 costs us 24\% of our
makespan and costs them 79\% of theirs; row deduplication is worth less to us
than their per-schema cache is to them. The case we assumed was our weakest is
the one we win by most, and it is the realistic one.

\paragraph{The mechanism, and the boundary of the result.} Varying the model
size decomposes the whole comparison. With semantics matched at batch 512, the
grammar's cost per generated token is:

\begin{center}
\begin{small}
\begin{tabular}{lrr}
\toprule
 & engrain & XGrammar \\
\midrule
Qwen3-0.6B & \textbf{0.054\,ms} & 0.114\,ms \\
Qwen3-8B & 0.064\,ms & 0.064\,ms \\
\bottomrule
\end{tabular}
\end{small}
\end{center}

Ours is nearly constant across model sizes. \textbf{Theirs halves.} Their fill
runs on the CPU and vLLM overlaps it with the forward pass: a 0.6B decode step
is too short to hide it, an 8B step is long enough to hide most of it. Ours
runs on the same device as the model and cannot be hidden---only made small. At
8B the speed difference is gone (7.04\,s against 6.86\,s) and only the
correctness difference remains (192 valid against 134).

This is the honest boundary of the throughput claim, and it also says what
residency is \emph{for}. A device-resident parser wins on speed when the model
is cheap relative to the mask and the batch is over a few dozen; it wins on
correctness, on capture, on fused sampling and on speculative decoding
regardless, because those are not latency properties.

Vocabulary size moves the crossover the same way: with opt-125m's 50{,}272
tokens the mask does not cross until about batch 128, where Qwen3's 151{,}669
crosses at 30, because XGrammar's per-row cost scales with the vocabulary and
ours is launch-bound at small batch. At batch 512 it reverses and we win most
on the smallest vocabulary, 7.1$\times$.

\subsection{Requests that arrive over time, and a defect it found}
\label{sec:eval-online}

Every measurement above submits its whole batch at once and waits, which is a
throughput experiment. It hides the two numbers an operator is judged on ---
time to first token and how evenly the rest arrive --- and it hides the case
this engine's admit/release design exists for, a batch whose composition
changes because requests are joining and leaving rather than starting together.
So we drive the engine with Poisson arrivals, each request carrying its own
schema, and record per-request latency (Table~\ref{tab:online}).

\begin{table}[t]
\caption{Poisson arrivals into vLLM's async engine, Qwen3-0.6B, 400 requests
over 32 schemas, 96-token cap. Milliseconds. Source: \texttt{rigor.online}.}
\label{tab:online}
\vskip 0.1in
\begin{center}
\begin{small}
\begin{tabular}{llrrrr}
\toprule
offered & engine & served & TTFT p50 & TTFT p99 & ITL p50 \\
\midrule
16 q/s & none & 15.1 & 18.9 & 48.3 & 3.2 \\
 & engrain & 14.9 & 27.4 & 547.5 & 6.1 \\
 & XGrammar & 15.0 & 22.3 & 78.2 & 3.2 \\
 & llguidance & 15.1 & \textbf{21.4} & \textbf{43.6} & 3.2 \\
\midrule
64 q/s & none & 56.4 & 29.3 & 52.0 & 3.2 \\
 & engrain & 53.7 & 46.2 & 1{,}338.7 & 9.9 \\
 & XGrammar & 52.4 & 69.5 & 120.9 & 21.8 \\
 & llguidance & 56.1 & \textbf{35.3} & \textbf{54.1} & 4.7 \\
\midrule
256 q/s & none & 125.0 & 330.7 & 1{,}018 & 5.2 \\
 & engrain & 75.0 & 1{,}726 & 2{,}711 & \textbf{10.1} \\
 & XGrammar & 68.5 & 1{,}125 & 2{,}998 & 25.5 \\
 & llguidance & \textbf{100.3} & \textbf{520.8} & \textbf{1{,}617} & 20.7 \\
\bottomrule
\end{tabular}
\end{small}
\end{center}
\vskip -0.1in
\end{table}

\textbf{We are last on time to first token at every rate}, and it is the cold
path of \S\ref{sec:costs}: a schema nobody has compiled costs 227\,ms before
its first mask, and under Poisson arrivals that lands in the tail of whichever
request drew it. \textbf{Inter-token latency is the one we win}, and only where
the batch is large enough for the mask to matter: 9.9\,ms against XGrammar's
21.8 at 64\,q/s, and at saturation 10.1 against llguidance's 20.7 and
XGrammar's 25.5. Sustained throughput at saturation is llguidance 100\,q/s,
engrain 75, XGrammar 68.5, against an unconstrained 125.

\paragraph{And it found a defect the six verifications do not reach.} With
per-row verification enabled, and only under continuous batching:

\begin{center}
\begin{small}
\texttt{row 6 (grammar 31): 0 words with extra bits, 12 with missing}\\
\texttt{same row alone: DIFFERS from the batch; alone vs matcher: agrees}
\end{small}
\end{center}

Computed alone the device agrees with the reference matcher; computed in the
batch it is missing twelve words of bits, which is a mask too narrow for the
row it belongs to. Three of 400 requests died outright. Our \texttt{mixed}
verification changes the grammar assignment between recording a graph and
replaying it, but every row is present for the whole run --- only arrivals and
departures give rows that join and leave \emph{mid-flight}, and that is the
shape none of the six cover. The correctness results of
\S\ref{sec:correctness} walk the host reference matcher and are unaffected, but
the serving numbers in this section are measured on an engine with a known
wrong-mask path, and we report them with that stated rather than withheld.

\subsection{Speculative decoding}
\label{sec:eval-spec}

A draft of $k$ tokens must be checked against the grammar and the accepted
prefix kept. This is the case where a host-side matcher stops being merely
costly. XGrammar offers \texttt{traverse\_draft\_tree}, which walks the whole
draft tree in one call and is close to flat in $k$; comparing against its
per-position path instead --- as an earlier version of this work did --- overstates
the gap by roughly 46$\times$ and we do not do so.

Charging every position a fill and an advance and the rollback too, we cost
102/219/361\,\textmu{}s at batch 128 for $k=1/4/8$ against 260/562/485, and
278/474/730 at batch 512 against 1{,}072/1{,}999/1{,}980 --- 1.35--4.22$\times$.
We win at every $k$ measured, but the trend is against us: our cost grows with
$k$ while theirs is roughly flat. Extrapolating,
the advantage closes near $k\approx16$. The structural point survives ---
our walk is one recorded graph replayed once, with rollback on device, and needs
no synchronization per position --- but the arithmetic advantage is finite and we
say so.

\subsection{The two strongest counter-arguments}
\label{sec:eval-counter}

Per-step numbers invite two objections, and both are stronger than any ratio we
can quote. We measured them rather than argued with them.

\paragraph{``XGrammar overlaps its fill with the forward pass, so residency buys
nothing.''} This is the objection that once forced this project to withdraw a
claim. We measure a real decode forward pass, each engine alone, and the two
together on one stream, and charge each engine only the time it \emph{adds} to
the step. On forward passes of 6.6 / 9.4 / 22.1\,ms at batch 32 / 128 / 512, we
add 44 / 60 / 90\,\textmu{}s and XGrammar adds 46 / 112 / 398. At batch 32 the
objection holds --- that is parity. It stops holding as the batch grows, because
what overlaps is bounded by the forward pass and XGrammar's cost is not:
4.42$\times$ at batch 512.

\paragraph{``The host is idle anyway.''} A host-side matcher is cheap when it
has a core to run on. Serving hosts do not offer that guarantee: the same CPUs
run detokenization, scheduling, request parsing and the API server. Sweeping 0/8/16/24 busy cores next to the fill at batch 128, our p50 and p99
are flat --- 27.8 to 27.9\,\textmu{}s and 51.0 to 56.7 --- because the work is
on the device. XGrammar's median degrades mildly (406.5 to 444.7,
1.09$\times$) but its \textbf{p99 goes from 449.6 to 3{,}495.3\,\textmu{}s, a
7.8$\times$ tail blow-up}. Constrained decoding is a tail-latency business, and
this is the clearest measurement in the paper of what residency is for.

\subsection{Ablation: what each mechanism is worth}
\label{sec:eval-ablation}

Table~\ref{tab:ablation} removes one mechanism at a time. Both knobs are set
\emph{before} the step is captured, which matters more than it sounds: our
first attempt set them afterwards and reported that the memo was worth
1.00$\times$ while hitting 90\% of steps --- the captured graph had the old
scalar arguments baked in, so nothing had actually been disabled. An ablation
of a graph-resident system has to be recorded, not configured.

\begin{table}[t]
\caption{Ablation on a three-schema batch with sequences at random document
positions. ``memo'' compares the full memo against a single-slot one; at batch
1 that is degenerate, since one slot always serves one sequence.
Source: \texttt{exp\_ablation}.}
\label{tab:ablation}
\vskip 0.1in
\begin{center}
\begin{small}
\begin{tabular}{rrrrr}
\toprule
batch & full & memo worth & capture worth & memo hit \\
\midrule
1   & 49.8\,\textmu{}s    & 1.00$\times$\,$^\dagger$ & 2.56$\times$ & 100.0\% \\
8   & 59.6\,\textmu{}s    & 6.15$\times$ & 2.34$\times$ & 100.0\% \\
32  & 408.6\,\textmu{}s   & 1.13$\times$ & 1.04$\times$ & 93.8\% \\
128 & 448.7\,\textmu{}s   & 1.16$\times$ & 1.04$\times$ & 90.6\% \\
512 & 3{,}980.4\,\textmu{}s & 1.00$\times$ & 1.00$\times$ & 86.1\% \\
\bottomrule
\end{tabular}
\end{small}
\end{center}
\vskip -0.1in
\end{table}

Both pay in the same place and for the same reason: at small batch the step is
dispatch-bound and mostly redundant, so capture is worth 2.3--2.6$\times$ and
the memo up to 6.2$\times$; at batch 512 both are 1.00$\times$ because the step
is work-bound and the work is genuinely different per sequence. We include the
negative half because a mechanism that helps only at batch 8 should be described
that way.

The third ablation is the fill split (\S\ref{sec:fill}), and it is the one that
justifies the paper's implementation lesson: on a balanced batch the single
fused kernel is fine, and on a skewed one it costs 9{,}883\,\textmu{}s against
691\,\textmu{}s. The regression is invisible to any benchmark that does not
seed sequences apart.

\subsection{Fused sampling}
\label{sec:eval-sampling}

Measured in place at batch 512, against mask-then-sample: 7.73$\times$ when
every row is sparse, 0.95$\times$ when every row is dense, 1.79$\times$ for a
shortlist that emits whichever of the allowed set and its complement is shorter,
and \textbf{1.05$\times$ on a ragged batch that is half dense} --- which is the
measured steady state. A fixed-shape sync-free variant is 0.86$\times$.

This is the result we most want to be different. Fused
sampling is the cleanest consequence of residency --- constraining makes sampling
\emph{cheaper}, which is impossible if the constraint is a host product --- and on
a homogeneous sparse batch it delivers 7.7$\times$. But a continuously batched
workload is about half dense rows, and there the advantage is parity.

We report the cause precisely because it is not in our engine. Two interfaces
prevent the ragged case from paying: XGrammar's
\texttt{apply\_token\_bitmask\_inplace} takes its row indices as a host
\texttt{List[int]}, and a sampler's output shape is its row count, so selecting a
subset of rows requires knowing that count on the host. What is needed is a
sampler that accepts a device-side row count. Until sampling APIs offer one, the
fused-sampling benefit is available in principle and 5\% in practice on a mixed
batch.

\subsection{Correctness, in two layers}
\label{sec:correctness}

Constrained decoding admits no approximation: one wrong bit either leaks an
illegal token or removes a legal one, so learned or lossy mask representations
are out of scope by construction. But ``correct'' names two different
properties, and conflating them is how this literature reports numbers that
cannot be acted on:

\begin{description}
\item[runtime exactness] the device mask equals what the compiled grammar
allows;
\item[lowering fidelity] the compiled grammar equals the schema.
\end{description}

The first is what residency depends on and we verify it exhaustively. The
second is where every grammar-based system loses ground, and it is where we
found --- and fixed --- a real defect in our own compiler.

\paragraph{Runtime exactness.} Every device mask row is compared bit-for-bit
against an independent CPU reference matcher built from the same tables, over
four verifications: \textbf{corpus} (619 rows on model-generated documents),
\textbf{walk} (4{,}800 rows on random walks, which reach states real documents
do not), \textbf{conflicted} (778 rows on grammars with reduce/reduce
conflicts, where forking is live), and \textbf{mixed} (1{,}092 rows on batches
of several grammars, including a CUDA graph \emph{recorded on one assignment of
grammars to sequences and replayed on a different one}). All 7{,}289 rows match,
zero failures. The graph-replay check is the one the residency claim rests on:
a recorded step survives the batch composition changing underneath it, which is
what continuous batching does every step. 99 unit tests also run under all
three backends, and the \texttt{differential} backend compares the CUDA and
Triton implementations on every call.

\paragraph{Lowering fidelity needs common ground, and there is none.}
The second property cannot be measured by comparing two engines on arbitrary
schemas, because they do not lower them to the same language. We looked for a
schema on which engrain and XGrammar agree token for token: there is none. Of
200 corpus schemas, and of 64 written specifically to be simple enough,
\textbf{zero} agree in any whitespace configuration. The differences are not
subtle---we admit a carriage return, which RFC 8259 says is whitespace and
their \texttt{any\_whitespace} omits; they admit whitespace after the document
where we do not.

So we construct the common ground instead. \texttt{rigor.fragment} rewrites
each of the 533 JSONSchemaBench schemas by applying the remedies our own
compiler reports---close the objects, move a keyword inside the branches it
constrains, delete what no level lowers---and keeps it only if the result lands
inside the fragment engrain enforces with nothing left over. Membership is
\emph{measured}: the schema is compiled, its relaxation list must be empty, and
then twenty documents are generated under its mask and handed to a validator.
468 schemas survive; 10{,}177 further walks under independent seeds produce
\textbf{zero} invalid documents.

Five schemas pass the first test and fail the second---a \verb|$ref| beside an
\texttt{allOf} beside a \texttt{oneOf} reaches a relaxation nobody modelled.
Predicting those means re-implementing the front end inside the reporter, which
is the duplicate model that put the bug there in the first place. Generating
documents and reading them does not.

\paragraph{On that fragment, XGrammar has two settings and neither is both.}
Table~\ref{tab:soundness} is the head-to-head. \emph{Over-acceptance} generates
under each mask and validates the result; \emph{under-acceptance} takes the
corpus's own documents, permutes their object keys, keeps the permutations the
schema still accepts, and asks whether the mask admits them.

\begin{table}[t]
\caption{Correctness on the fragment engrain enforces exactly. Over-acceptance
is documents generated under the mask that the schema rejects; under-acceptance
is valid documents, with their object keys permuted, that the mask refuses.
Source: \texttt{rigor.fragment}, \texttt{rigor.soundness}.}
\label{tab:soundness}
\vskip 0.1in
\begin{center}
\begin{small}
\begin{tabular}{lrr}
\toprule
 & over-accepts & rejects a valid doc. \\
\midrule
\textbf{engrain} & \textbf{0.0\%} (0/2{,}115) & \textbf{0.0\%} (0/390) \\
llguidance & \textbf{0.0\%} (0/2{,}421) & 91.5\% (357/390) \\
XGrammar, ordered & 6.0\% (153/2{,}551) & 91.7\% (385/420) \\
XGrammar, unordered & \textbf{26.3\%} (646/2{,}452) & 0.7\% \\
\bottomrule
\end{tabular}
\end{small}
\end{center}
\vskip -0.1in
\end{table}

XGrammar's \texttt{any\_order} flag is not exposed by vLLM, so every deployed
comparison runs its \emph{ordered} lowering, in which an object's properties
must arrive in the order the schema declares. That is narrower than JSON
Schema: a valid document whose keys are permuted is refused nine times in ten.
The unordered lowering is the one that means what the schema means, and there
\texttt{required} is not enforced at all---485 of its 646 failures are a
missing required property.

\textbf{llguidance is sound}, and this is the result that keeps the claim
honest: it enforces \texttt{required} correctly and admits nothing the schema
rejects over 2{,}421 complete walks. XGrammar is the outlier, not the rule.
llguidance pays for it the other way --- it fixes object property order in its
lowering, and unlike XGrammar this cannot be configured away. Its JSON options
are \texttt{item\_separator}, \texttt{key\_separator},
\texttt{whitespace\_flexible}, \texttt{whitespace\_pattern},
\texttt{coerce\_one\_of}, \texttt{lenient} and
\texttt{json\_allowed\_escapes}: there is no ordering option at all.

So the claim is exactly one sentence, and not a word more. \textbf{Engrain is
the only engine here that is both sound and order-free.} It is not the only
sound one. Enforcing \texttt{required} on an unordered object is what the
subset construction of \S\ref{sec:automaton} is for, and the two baselines
between them show why that is the hard combination rather than either half:
one gives up soundness to get order-freedom, the other gives up order-freedom
to keep soundness, and there is no configuration of either that has both.

\paragraph{The residual 6\% is one bug, and we had it too.} 148 of XGrammar's
153 ordered-mode failures are not schema violations at all---they are documents
\texttt{json.loads} refuses, because a pattern's \verb|.| matched a raw control
character. A regex has no idea it is being read inside a JSON string, and
RFC 8259 requires the quotation mark, the reverse solidus and U+0000--U+001F to
be escaped. Constructing this corpus found the same bug in \emph{our} front end
the same morning; we fixed it by rewriting a pattern's character classes so
they can only match string content, keeping the forbidden characters by
spelling them the way JSON spells them---dropping them would narrow, which the
contract forbids. It is isolated: on a plain \verb|{"type":"string"}| both
engines correctly refuse a raw tab; inside \verb|pattern| only engrain does.

\paragraph{The differences are in the front end, not the parser.} On grammars
that do not go through a schema, the two engines are equivalent.
\texttt{rigor.nonjson} runs 26 regexes and 12 EBNF grammars---from dates,
UUIDs and HTTP request lines to self-recursion, 64 levels of nesting, a
left-recursive list, $a^n b^n$, an ambiguity past one token of lookahead, and a
512-way alternation. A regex has a referee neither engine wrote:
\texttt{re.fullmatch} adjudicates over-acceptance, a set of matching strings
adjudicates under-acceptance, and each engine's generated strings are handed to
the other. Both compile 38 of 38; both over-accept nothing; both admit every
positive case and refuse every negative one; neither refuses anything the other
generates.

One difference survives, and it is at the byte level: on
\verb|[\u0100-\u2000]+| XGrammar admits \verb|\xe0\x84\x81|, which is U+0101
spelled in three bytes where two suffice. RFC 3629 forbids an overlong encoding
and it is the classic way past a filter that inspects the decoded string.
Truncated sequences, lone continuations, surrogates and bad tails are refused
by both.

We therefore attribute the \texttt{required} and ordering results above to
\emph{how a schema is lowered}, not to the parsing machinery---which is the
useful form of the claim, because a front end can be fixed and an automaton
class cannot.

\paragraph{Declared exactness.} A relaxation the caller is not told about is
worse than one they are. Every grammar now reports what it does not enforce, as
a record naming the keyword, a JSON pointer to the place, what the mask admits,
and the edit that would enforce it. The list is validated the same way as
everything else: over 200 corpus schemas and 2{,}003 walks, \textbf{100\%} of
over-accepted documents are attributable to an entry in it. Reaching 100\%
required three fixes---\texttt{anyOf} declared unconditionally, keywords driven
from the specification's vocabulary rather than from cases we remembered, and
\texttt{allOf} reported on the object it merges to rather than branch by
branch---and the last of those was a schema that reported \emph{nothing}
relaxed while admitting a document missing nine required names.

\subsection{The costs of residency}
\label{sec:costs}

Residency is not free, and the honest accounting is the following.

\paragraph{Memory, and a number that was wrong.} Our own accounting undercounted
what the arena holds: it omitted the readings and the verdict table and charged
a flat 20 bytes for a group, reporting 2.9\,MB a schema against a real 7.5. A
budget denominated in less than the truth is not a budget---a 1\,GiB one was
holding what a 2.6\,GiB one had been asked for, and the eviction that followed
looked like a mystery. Counted from the arrays actually uploaded, and checked
against them to 100.0\%, 409 fragment schemas cost 4.0\,GiB.

Two changes bring that down by 2.05$\times$ (Table~\ref{tab:costs}). Every
grammar in a pool is compiled against one tokenizer, so a block two of them
hold is the same words twice; both large stores are content-addressed now, with
absolute offsets and refcounted blocks. A group's list of ways to read its
tokens is the same list in state after state---4.8$\times$ within one grammar,
6.9$\times$ across a pool. And the verdict table, which says whether a
(lexer state, parser state) pair admits a group without replaying the group's
readings, is a shortcut the kernel does not need: dropping it costs 5.9\% of
the mask and a quarter of the arena.

\begin{table}[t]
\caption{Arena for 150 fragment schemas, Qwen3-0.6B, 151{,}669 tokens.
Source: \texttt{engrain\_lab.rigor.cost} and a live pool.}
\label{tab:costs}
\vskip 0.1in
\begin{center}
\begin{small}
\begin{tabular}{lrr}
\toprule
 & arena & mask, batch 512 \\
\midrule
as measured & 1{,}406.6\,MiB & 1{,}894\,\textmu{}s \\
blocks shared & 867.1\,MiB & 1{,}894\,\textmu{}s \\
\quad + verdicts dropped & \textbf{659.8\,MiB} & 2{,}005\,\textmu{}s \\
\bottomrule
\end{tabular}
\end{small}
\end{center}
\vskip -0.1in
\end{table}

\paragraph{It is the same bytes, in a worse place.} Measured on the same 409
schemas, our arena is 1{,}965\,MiB of device memory and XGrammar's is
2{,}010\,MiB of host RSS, plus 29\,MiB once every grammar has filled one mask
and their cache has warmed. Almost identical. What differs is where they sit:
ours competes with the KV cache. The statement is ``we pay in VRAM'', not ``we
use an order of magnitude more'', and we had the latter as an estimate before
we measured it.

\paragraph{The distribution is the remaining lever.} Per-schema table size over
200 fragment schemas is p50 3.66\,MiB, p90 25.19, p99 85.62, max 286.6: the top
tenth of schemas holds \emph{half} the arena. A budget that refuses, degrades
or pages those is worth more than another constant factor on the median, and
\texttt{footprint()} already predicts the size before it is paid.

\paragraph{The cold path is our worst number, and llguidance shows how bad.}
From a schema nobody has seen to its first usable mask, at the median:
\textbf{llguidance 1.2\,ms}, XGrammar 20.7, engrain 227.2, of which 98\,ms is
our compiler and the rest is preparing and admitting the tables. In serving,
schemas arrive with requests, so this is a stated failure rather than a
footnote. XGrammar's tail is worse than ours---p99 4.0\,s against 8.9, worst
case 11.9\,s against 54.5---but llguidance's is bounded at \textbf{11.5\,ms},
because an Earley parser needs almost no precomputation. It sits at the lazy
end of exactly the memory-for-compute curve our arena sits at the eager end of,
and it is the strongest argument for the lazy residency below.

\paragraph{Compile time} is \emph{linear} in the number of lexer states (slope
1.05, $r=0.94$): there is no algorithmic blow-up, only the inherent
$O(|V| \times \text{lexer states})$ cost of computing what every token does
from every lexer state. The one lever with the right shape is building a lexer
state's tables \emph{on demand}, the first time a parse reaches that state: a
real document enters 4.3\% of the states its grammar can reach and touches
23.4\% of the token sets. That would address compile time and memory with one
mechanism, and it is the main piece of future work.

\paragraph{Exactness of what.} We report bit-exactness against \emph{our
compiled grammar}, which is weaker and more precise than bit-exactness against
the schema; \S\ref{sec:correctness} measures the difference. We believe every
system in this space owes that distinction.

\section{Limitations}
\label{sec:limitations}

Stated as results, because the structural claim survives them and a reader
deciding whether to deploy needs them. \textbf{llguidance beats us end to end up to
batch 128} and serves a single request in half our time; the crossover against
it is about batch 160, where against XGrammar it is 30. Our floor is set by
sweeping live configurations, not by the batch. \textbf{llguidance is also
sound}: it admits nothing its schema rejects, so our correctness claim is
order-freedom, not soundness. \textbf{The cold path is 227\,ms} from a schema
nobody has seen to its first usable mask, against llguidance's 1.2 and
XGrammar's 20.7 --- 190$\times$ off the best, and llguidance's worst case is
11.5\,ms where ours is 11.9\,s. Schemas arrive with requests, so this is the
number a first request waits on. Lazy lexer residency is the plausible fix and is
unimplemented. \textbf{Our arena is the same size as XGrammar's host cache} --- 1{,}965\,MiB
against 2{,}010 over 409 schemas --- but it sits in device memory and competes
with the KV cache, and a tenth of the schemas hold half of it.
\textbf{Our throughput advantage is a function of how cheap the model is}: at
Qwen3-8B the grammar costs both engines 0.064\,ms per token and the speed
difference disappears, because a host fill hides behind a long forward pass and
a device fill cannot. \textbf{The corpus we measure correctness on is
favourable by construction} --- it is the fragment where \emph{we} are exact
--- though the under-acceptance result is not, since it permutes the baseline's
own documents. \textbf{Fused sampling is
1.05$\times$ on a ragged batch}, not the 7.7$\times$ a homogeneous one
suggests. \textbf{Heterogeneous pools of 8--16 schemas cost us
33--36$\times$} what those schemas cost alone. \textbf{The memo and graph
capture pay only below batch 32.} \textbf{One GPU}: every launch shape, memo
size and chunk count was fitted on an A100. \textbf{A known wrong-mask path}:
under continuous batching a row's mask can come out narrower in the batch than
computed alone (\S\ref{sec:eval-online}); it is not root-caused, and it is the
first thing to fix. \textbf{Untested}: tensor parallelism, prefill-heavy
workloads, and a vocabulary above 200{,}000 tokens.

\section{Related Work}
\label{sec:related}

\paragraph{Host-side constrained decoding.} Outlines~\cite{willard2023efficient}
indexes a regular-language DFA against the vocabulary;
SynCode~\cite{ugare2025syncode} and grammar-aligned
decoding~\cite{park2024grammaraligned} extend to context-free grammars;
XGrammar~\cite{dong2024xgrammar} makes the pushdown automaton practical with
adaptive token masks and is the deployed baseline;
llguidance and Domino~\cite{beurerkellner2024domino} attack the same overhead
with better host algorithms and speculation. llguidance is the strongest of
these in our measurements and the one a reader should compare us against: its
Earley formulation needs no vocabulary-indexed tables, so it compiles in
milliseconds and its per-step cost is flat in the grammar
(\S\ref{sec:eval-step}). All compute the mask on the CPU, which is the
property this paper is about.

\paragraph{GPU-side work.} Pre3~\cite{chen2025pre3} and PSC~\cite{li2026psc}
precompute deterministic pushdown structure to make the host step cheaper or to
classify parser stacks; Gram2Token~\cite{hua2026gram2token} moves the
token-to-terminal bridge toward the device. These reduce the cost of the host
step or move part of it; to our knowledge none keeps the parser stack itself on
the device across steps, which is what makes graph capture and sampler fusion
possible.

\paragraph{Regular-language lowerings.} Outlines~\cite{willard2023efficient}
and \texttt{outlines\_core} lower a JSON Schema to a regular expression and
index a DFA against the vocabulary. The lowering is an under-approximation: the
emitted regex fixes object property order, so a document whose properties
arrive in a different order than declared is rejected although the schema
permits it. XGrammar's default shares this restriction but can be configured
out of it (\S\ref{sec:baselines}); a regex cannot.

\paragraph{Automata and tokenizers.} The tokenizer--grammar impedance mismatch
is treated by~\cite{koo2024automata} and~\cite{park2025flexible};
CRANE~\cite{banerjee2025crane} studies the expressiveness cost of constraining.
Our token-group quotient is a device-oriented instance of the same idea. CUDA
graph capture of the decode loop is standard in vLLM and
SGLang~\cite{zheng2024sglang}; our contribution is to make the grammar a
participant in it.

\section{Conclusion}

Constrained decoding has been treated as a host-side algorithms problem, and by
that measure it is nearly solved. We argued that the interesting question is
where the parser's state lives, because a decode step recorded as a CUDA graph
can only contain device work. Engrain keeps an LALR(1) parser --- lexer state,
stack, and conflict forks --- resident on the GPU, which makes the constraint
capturable, fusable with the sampler, and advanceable through a speculative
draft without a host round trip. It adds 90\,\textmu{}s to an overlapped step
where a host matcher adds 398, holds its tail flat under CPU contention where a
host matcher's p99 rises 7.8$\times$, and is bit-exact over 7{,}289 verified
rows.

It is also the only engine here that is both sound and order-free on the
fragment we could construct common ground on, which took building that fragment
and verifying it by generation rather than by argument --- a methodology we
think this literature needs, since it found four defects in our own front end
and two in a baseline's. llguidance is sound and its compiler is 190$\times$
faster than ours; what it cannot do is accept a valid document whose properties
arrive in a different order, and no setting changes that. And it costs: 236\,ms before a new schema's first mask
against 17, a loss below a few dozen concurrent requests, an arena in device
memory rather than host, and, on an 8B model, no throughput advantage at all
once the baseline's fill hides behind the forward pass. We believe the
structural claim is worth those prices, and we have tried to report them
precisely enough that a reader can disagree.

\bibliography{references}
\bibliographystyle{mlsys2025}

\end{document}
